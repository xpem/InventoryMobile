<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PersonalAssetsMobile.Components.FlyoutHeader" 
             x:Name="this"
             >
    <FlexLayout HeightRequest="100" BackgroundColor="{StaticResource TitleBarBackGroudColor}" JustifyContent="Center">
        <Label 
            Text="{Binding Source={x:Reference this},Path=Email}"
            VerticalOptions="Center" 
            HorizontalOptions="Center"
            TextColor="White"/>
    </FlexLayout>
</ContentView>
